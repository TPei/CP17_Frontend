import { MarkerClusterDirective } from './marker-cluster.directive';

describe('MarkerClusterDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkerClusterDirective();
    expect(directive).toBeTruthy();
  });
});
